.item
  .tyche-product.primary
    .tyche-product-image
      = image_tag (products.images.first.file_url if products.images.presence) || "/assets/user1.jpg", size: "45", alt: "Edit Entry", class: "attachment-shop_catalog.size-shop_catalog"
    .tyche-product-body
      h3
        a.woocommerce-LoopProduct-link.woocommerce-loop-product__link
          = "#{products.name}"
      = render "products/rate_product"
      span.price
        span.woocommerce-Price-amount.amount
          span.woocommerce-Price-currencySymbol
            | $
          = " #{products.price}"
      = render "products/add_cart"
